type CodemodOptions = {
  appName: string;
  filename: string;
  projectRoot: string;
};

type Options = {
  appName: string;
  filename: string;
  projectRoot: string;
};

export type { CodemodOptions, Options };
